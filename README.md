To build PyNvVideoCodec:

1) Download the latest version of the PyNvVideoCodec source from https://catalog.ngc.nvidia.com/orgs/nvidia/resources/pynvvideocodec
2) Run ./build.sh <source-path>

The resulting wheel will be located in the build/ directory.
